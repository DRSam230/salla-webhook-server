{
  "name": "salla-webhook-server",
  "version": "1.0.0",
  "description": "Salla webhook server for Excel integration",
  "main": "salla-webhook-server.js",
  "scripts": {
    "start": "node salla-webhook-server.js",
    "dev": "nodemon salla-webhook-server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
